# Ambiente: venv_yolo_ultralytics — pacote ultralytics (E02 YOLOv8 + E03/E04 YOLO11).
# A versão do detector vem do arquivo de pesos (yolov8s.pt, yolo11s.pt), não de outro venv.
# Não misturar com venv_yolo_5 (YOLOv5). Não criar venv separado para v8 vs v11.
# Python recomendado: 3.10 ou 3.11
#
# Setup: .\experiments\YOLO_V8\setup_venv_ultralytics.ps1
# GPU (Windows): .\experiments\YOLO_V8\setup_pytorch_gpu.ps1

ultralytics>=8.3.0,<9.0.0

# torch via pip no Windows costuma ser CPU-only (+cpu). Apos o setup, rode:
#   .\experiments\YOLO_V8\setup_pytorch_gpu.ps1
torch>=2.0.0
torchvision>=0.15.0

numpy>=1.23.0
pillow>=10.0.0
matplotlib>=3.8.0
PyYAML>=6.0
opencv-python-headless>=4.8.0
pandas>=2.0.0
